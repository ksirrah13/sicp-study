#lang sicp
(#%require "../lib/ch3-shared.scm")

(define (or-gate a1 a2 output)
  (let ((not-a (make-wire))
        (not-b (make-wire))
        (out-c (make-wire)))
    (inverter a1 not-a)
    (inverter a2 not-b)
    (and-gate not-a not-b out-c)
    (inverter out-c output)
    'ok))